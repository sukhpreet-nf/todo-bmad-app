# todo-bmad-app — Project Documentation Index

> Brownfield documentation generated by the BMAD `bmad-document-project` skill on **2026-04-29**.
> Mode: `initial_scan` · Level: `deep` · Owner: Sukhpreetbhatia

This index is the entry point for AI agents and humans onboarding to the codebase.
Files under `docs/bmad/` are the original planning artifacts (brief, PRD, architecture, stories, test strategy, AI log, QA notes) and remain authoritative for *intent*. Files in this folder describe the system **as it is actually built**.

## Quick links

| Topic | File |
|-------|------|
| Project overview & elevator pitch | [`project-overview.md`](./project-overview.md) |
| Runtime architecture | [`architecture.md`](./architecture.md) |
| Backend module (Express + SQLite) | [`backend.md`](./backend.md) |
| Frontend module (React + Vite) | [`frontend.md`](./frontend.md) |
| REST API contract | [`api.md`](./api.md) |
| Data model (SQLite schema) | [`data-model.md`](./data-model.md) |
| Local dev, test & deploy | [`development.md`](./development.md) |
| Original BMAD planning artifacts | [`bmad/`](./bmad/) |

## Project classification

- **Type:** Full-stack monorepo web application
- **Domain:** Personal task / todo management
- **Surface area:** 1 backend service + 1 SPA frontend, both containerized
- **Architecture style:** Client-Server, single SQLite write node, REST/JSON

## Tech stack at a glance

| Layer | Choice | Version |
|-------|--------|---------|
| Backend runtime | Node.js | 20+ |
| Backend framework | Express | ^4.18.3 |
| Backend persistence | better-sqlite3 | ^9.4.3 |
| Backend tests | Vitest + Supertest | ^1.3.1 / ^6.3.4 |
| Frontend framework | React | 18 |
| Frontend build tool | Vite | 5 |
| Frontend styling | Tailwind CSS | 3 |
| Frontend tests | Vitest + Testing Library + Playwright | latest |
| Container orchestration | Docker Compose v3.9 | — |
| Reverse proxy (prod frontend) | Nginx (in container) | — |

## Repository layout

```
todo-bmad-app/
├── backend/              # Express API + SQLite (port 3001)
├── frontend/             # React SPA (Vite dev :5173 / Nginx prod :80)
├── docs/                 # ← you are here (project knowledge)
│   └── bmad/             # original planning docs
├── _bmad/                # BMAD framework engine (do not edit)
├── _bmad-output/         # generated planning + implementation artifacts
├── .agents/skills/       # 42 BMAD skills exposed to AI tools
└── docker-compose.yml    # one-shot local stack
```

## Recommended next BMAD skills

Based on this scan and the existing `docs/bmad/04-stories.md` backlog:

1. **`bmad-help`** — orient on the next workflow step.
2. **`bmad-shard-doc`** — split `docs/bmad/02-prd-refined.md` and `03-architecture.md` into per-epic / per-component shards for easier story consumption.
3. **`bmad-create-story`** — draft the next implementation-ready story.
4. **`bmad-dev-story`** → **`bmad-code-review`** — inner dev loop.
